// Package version carries the build version, kept in step by bump-my-version.
package version

// Version is rewritten by `bump-my-version bump`; see .bumpversion.toml.
const Version = "0.1.0+260911"
